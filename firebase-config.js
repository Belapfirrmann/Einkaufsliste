/* Zugangsdaten der gemeinsamen Liste.
   Die Werte stammen aus der Firebase-Konsole:
   Projekt anlegen -> Web-App hinzufuegen -> Konfiguration kopieren.
   Sie sind keine Geheimnisse, wer die Liste aendern darf, regeln die
   Datenbank-Regeln (siehe README). Solange die Felder leer sind, laeuft die
   App rein lokal in diesem Browser. */
window.MARKTLISTE_FIREBASE = {
  apiKey: "",
  authDomain: "",
  databaseURL: "",
  projectId: "",
  appId: ""
};
